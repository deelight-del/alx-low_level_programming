#include <stdio.h>
#include "main.h"

/**
*add - adds two integers together
*@a: first operand to add
*@b: second operand to add
*
*Return: returns int addition of two numbers
*/

int add(int a, int b)
{
int ret;
ret = a + b;
return (ret);
}
