#include <stdio.h>
#include "main.h"
/**
*mul - returns the result of multipying two nummbers together
*@a: first operand
*@b: second operand
*
*Return: returns the result of multipying a and b
*/

int mul(int a, int b)
{
	int ret;

	ret = a * b;
	return (ret);
}
