#include <stdio.h>
/** main - returns 0 but prinss
*
*Return: returns int 0
*/

int main(void)
{
char i;
int j;
long long int k;
float l;
printf("Size of a char: %d byte(s)\n
	Size of an int: %d byte(s)\n
	Size of a long long int: %d bytess)\n
	Size of a float: %d byte(s)\n", sizeof(i), sizeof(j), sizeof(k), sizeof(l));
return (0);
}
